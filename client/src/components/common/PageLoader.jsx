const PageLoader = () => (
  <div className="flex min-h-[40vh] items-center justify-center">
    <div className="h-10 w-10 animate-spin rounded-full border-2 border-brand-gold/40 border-t-brand-gold" aria-label="Loading" />
  </div>
);

export default PageLoader;
